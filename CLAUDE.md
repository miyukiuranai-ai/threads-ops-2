# CLAUDE.md — threads-ops-2（検証環境）の方針

このリポジトリは Threads の複数名義（占い師）を Threads 公式 API だけで自動運用するツール。本家 threads-ops と同じ仕組みを、別の Firestore・別のバケット・別の Vercel・別の定期実行で動かす検証環境。本家との違いは画面の配色（黒）と名前（threads-ops（検証））だけ。

## 目的
- 目的は LINE の友だち追加。反響（表示・いいね）ではなく登録で判断する。
- 投稿文は Claude で生成し、人が承認してから投稿する。承認が無ければ 23:30 の自動仕分けで振り分ける。
- ブラウザの自動操作はしない。他人の投稿の検索・プロフィール取得は使わない（App Review をしない）。

## 運用の決まり（本人の言葉。docs/claude-memory にも同じ）
- いちばん見るのはインプレッション。判定はフォロワー・平均・中央値と比べない。全名義共通の絶対値: 3時間後の表示が 300未満は悪い、300〜1,000は普通、1,000〜3,000は良い、3,000以上はバズ。
- 集客（LINE 追加）が来ている名義には権威性があるので、超バズだけは必ず NG。落ちたら「超バズで様子見＋属人＋バズ特化」を全部入れる。超バズだけにしてよいのは一回も集客が来ていない名義だけ。
- 属人型は伸びにくいのが基本。属人型は貴重。1名義の底は 1日 4〜13件の登録、そこに属人のバズで 20〜30件が乗る。名義を横に増やして底を積む。
- LINE の実データ: 純バズのコメントは登録率 3%、属人（名乗り・霊視開始・暦）のコメントは 20〜40%。4本に増やして同じ書き出しを繰り返した 2〜3日後に表示が 1/10 に落ち、登録も落ちる。
- 参考は「この程度」で使う（兵庫版→大阪版）。名義の色を出しすぎると属人の意味がなくなる。
- 数字は 0〜9 の数字。飾りの記号は禁止。合言葉は絵文字 1つか 2文字以内。
- 同じ文面・同じ画像の使い回しは構わない。同じ画像を別の名義で使うのも可。

## 投稿の型（8つ、3段）
- 超バズ特化: image_buzz（画像が主役、1〜4行、必ずストックの画像）
- バズ特化: buzz_engagement、reading_open
- 属人: attract_intro、personal_note、shrine_visit、travel_note、exclusion_hook
型ガイドの文言は lib/server/post-types.mjs。生成の共通ルールは lib/server/prompts.mjs（SYSTEM）。

## 1日の組み立て（lib/server/pipeline.mjs）
指示（directives）＞ dayMix（比率）＞ 落ち込み ＞ postsPerDay の順で本数を決め、bands から枠を選び、型を日ごとにランダムに置く。超バズの差し込み規則、合言葉の交互、暦、口コミは pipeline.mjs を読む。

## 定期実行
- Vercel Cron: 15:30 JST の生成（前日レポート → 翌日ぶん）、21:00 UTC のトークン延長。
- 5分おき（外部）: /api/cron/publish（自動仕分け → 自動承認 → 投稿 → 投稿ごとの切り替え → 反応取り込み → 1h/3h → 反応0の削除）と /api/cron/replies。

## 安全と費用
- 投稿は必ず guard を通す（POSTING_MODE、名義の status、トークン期限）。名義単位で例外を閉じ込める。
- 秘密情報はコードに書かない。.env.local は git に含めない。
- 本家の Firestore・バケット・Cron・Apps Script には一切触れない。

## コードの決まり
- Next.js 15 App Router、JavaScript、"type": "module"。lib/server/*.mjs は Next に依存しない（コマンドからも使う）。
- 時刻は ISO（UTC）で保存し、表示と枠は日本時間（lib/server/time.mjs）。
- 画面の文言は日本語の運用者向け。

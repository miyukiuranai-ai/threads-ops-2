# threads-ops — Claude への共通の指示

この案件を複数人（uta / suzuki）がそれぞれの Claude Code から触るため、
運用の方針と決まりごとをここに置く。どの端末のセッションでも同じ前提で動くこと。

## 何のツールか

- Threads の複数名義を、Threads 公式 API だけで自動運用する（自分の名義をブラウザ自動化しない）。
- Next.js（App Router）を Vercel Hobby にデプロイ。Firestore にデータ、Anthropic API で文章生成。
- 秘密情報は `.env.local` と Vercel の環境変数だけに置く。コードに書かない。チャットに貼らない。
- Threads の App Review は行わない（他人の投稿の自動収集はしない）。競合の分析は人がチャットで行い、監視リストに残す。

## 運用の最大の軸（本人の方針。変えない）

- 「この人だからこそ頼りたい」と思わせる。不安を言葉にしてから肯定で受け止める。
- 最終目的は LINE の登録。反響の数ではなく登録で判断する。
- いいねだけを集める投稿は LINE に効かない（実体験）。コメントが付き、返信を重ねると登録につながる。
- コメントは求めるが求めすぎない。「○○を置いて」と合言葉を指定するのは1日2〜3本まで（前日と交互）。
  「お声をくだされば」「生年月日を残して」「好きな絵文字を」は指定しない側。
- 合言葉は絵文字1つか2文字以内の身近な語。固有名詞や読めない漢字は禁止。
- アスタリスクや ✴︎ で語を囲む強調は絶対禁止（【】「」で囲うのは可）。改行は意味の切れ目だけ、空行は2つまで。
- Threads はバズと落ち込みを繰り返す。落ち込んだら本数を減らす（自動判定あり）。良くても1日3〜4本を超えない。
- 星蘭はいまの型が当たっているので、属人型の共通指示は触らない。
- 暦（開運日カレンダー）は「日付と時刻を積む型」を書くときだけの材料。毎日入れない。

## 誰が何をするか（役割）

- **ツールのコードを変えるのは uta だけ。** コードの編集、コミット、push、デプロイは uta のセッションでのみ行う。
- suzuki は**運用のすべて**を行える: 投稿の生成・作り直し・本文の決定、キャラ設定の変更、レポートの確認、相談。ツールの中の「相談」からも同じことができる。
  コードを編集しない。`git commit` / `git push` をしない。もし変更が必要だと思ったら、変更内容を文章で uta に伝える。
- 方針の判断メモ（docs/claude-memory）も、書き換えは uta が行う。suzuki は提案として伝える。

## 運用の命令（Claude Code から。API の費用をかけない）

運用の指示は、ツールの「相談」ではなく Claude Code から `npm run ops` で行う（定額の中で済む。生成のときだけ API の費用がかかる）。
コードは触らず、次の命令だけを使う。名義は @ユーザー名でも名前でもよい。

```bash
npm run ops -- list-drafts --account uta001012 --date 2026-09-12
npm run ops -- directive --account uta001012 --action one_buzz          # rest / one / one_buzz / two / keep
npm run ops -- generate --account uta001012 --types attract_intro,buzz_engagement --slots 07:20,21:00 --replace --instruction "合図は🙏。暦は使わない"
npm run ops -- edit-draft --account uta001012 --time 23:47 --body-file ./body.txt --keyword 🙏
npm run ops -- persona --account uta001012 --set askPerDay=2-3 --set buzzTrial=true --set "bands=21:00-01:00|04:00-09:00|17:00-20:00"
npm run ops -- lines --bad 300 --good 1000 --buzz 3000                  # 表示数の良い・悪いの線（全名義共通）。引数なしで今の線
```

- 直前の投稿の判定で次の型を決める（本人 9/14）: 悪い→超バズ特化かバズ特化／普通→バズ特化（合図ありでも可）／良い・バズ→属人。属人型で 1,000 付いたらバズ扱いで属人を続ける。雅は当分、超バズ特化のみ。1日では早すぎるので、2日つづけて 1,000 を超えない名義に超バズ特化を1本入れる（星蘭で試験中。うたは手動なので結果を見て導入）。この表は鈴木の名義にも当てる。3日つづけて 300 に届かない名義は、雅と同じく超バズ特化だけにする（自動。ただし集客が来ている名義＝直近30日に LINE 追加か 1,000 以上の投稿がある名義には必ず当てない。落ちたら超バズ 1本＋属人＋バズ特化を全部入れる）。
- 表示数の良い・悪いは、フォロワー数や名義の平均・中央値と比べない。全名義共通の線（3時間後の表示: 300未満=悪い／1,000以上=良い／3,000以上=バズ）で判定する（本人 9/14）。

- 誰が操作したかは環境変数 `OPS_USER`（既定 suzuki）で記録される。suzuki の PC では `.env.local` に `OPS_USER=suzuki` を書く。
- 型のキー: attract_intro（属人型）/ travel_note（土地移動型・属人系）/ exclusion_hook（除外フック型）/ reading_open（霊視開始型）/ buzz_engagement（バズ型）/ image_buzz（画像バズ型・画像ストックから自動で添える。雑魚の名義向け）。
- レポートを読むときは `npm run report -- --date YYYY-MM-DD --dry`（保存しない）。投稿の試し打ちは `npm run gen -- --account 名義 --date 日付 --dry`。
- 昨日の分析をもとに「落ちているので属人型を減らしてバズ型に」「伸びているので属人型を2本に」のような判断は、この命令で反映する。

## コードの決まり

- コメントと画面の文言は日本語。読み手は開発者でない運用者なので、専門用語を避け、なぜそうするかを書く。
- `lib/server/*.mjs` がサーバー側の本体。`app/_actions/*.js` がサーバーアクション、`app/api/*` が長い処理の入口。
- 生成の指示は `lib/server/generate.mjs`、1日の組み立ては `lib/server/pipeline.mjs`、時刻は `lib/server/schedule.mjs`。
- `next build` は開発サーバーを止めてから実行する（同時に走らせると `.next` が壊れる）。
- 変更は小さくコミットし、`main` に push すると Vercel に自動デプロイされる。同時に触るときは枝（branch）を切って PR で合流する。
- Firestore のドキュメント ID に `__x__` の形を使わない（予約されている）。
- 名義ごとの設定は Firestore の `personas`。画面のキャラ設定から変えられるものは、コードに固定しない。

## 運用の手順書

- `docs/OPERATOR.md` に画面の使い方がある。新しい機能を足したら、そこにも一段落足す。

## これまでの経緯（記憶の共有）

- `docs/claude-memory/` に、これまでのセッションで積み上げた判断のメモを置いている（方針、評価の物差し、名義ごとの方向、参考にした競合）。
  新しい端末やセッションで作業を始めるときは、まず `docs/claude-memory/MEMORY.md` から読む。
- 方針が変わったら、このメモも更新してコミットする（人が読んでも分かる日本語で）。

## 型の線引き（本人 9/12。この3段で考える）

| 段 | ツールの型 | 使う名義 |
| --- | --- | --- |
| 超バズ特化型 | image_buzz（画像が主役、本文1〜4行、画像ストックから自動） | 雑魚の名義だけ（雅）。属人型で回している名義は本当に伸びなくなったときだけ |
| バズ特化型 | buzz_engagement（朝なので言いますが…、日付と時刻を積む型）。霊視開始型もこちら側 | うた、星蘭、玲月など |
| 属人型 | attract_intro（名乗り・由来）。土地移動型もこちら側 | 集客の本体 |

うたはバズ特化型と属人型しか使わない。超バズ特化型の参考アカウントは、その型の投稿だけを参考にし、普通の型は参考にしない。

## 名義の段階（本人 9/12。運用の基本の考え方）

1. **いちばんきれいなのは属人型だけ**で回すこと。
2. 落ち込みを想定して、**バズ特化型を入れつつ属人型で集客**する。うたのように一度上がった名義はここまで。超バズ特化型は要らない。バズ特化型で立て直して属人型に戻す。
3. **弱すぎる名義は属人型を打っても意味がない**ので、**超バズ特化型も織り交ぜる**（玲月、雅）。
- 星蘭は超バズ特化型は不要（バズ特化1本＋属人1本）。
- バズ特化型の分かりやすい見本は @mikoto_uranaishi（5,631人。早朝の暦型を毎日）。
- 超バズ特化型のお手本は超バズ特化型の枠にしか渡らない。うた・星蘭の生成には混ざらない。

## 型の全体像（本人が 9/12 に全部出したもの。お手本は型ごとに登録済み）

| 段 | 型（キー） | 中身 |
| --- | --- | --- |
| 超バズ特化 | image_buzz | 画像が主役。いいねするだけで◯◯🐉／飛ばしたらダメ『絵文字』／くっつけ2枚／素通り以外◯分以内／日付＋見れた人だけ。雑魚の名義だけ |
| バズ特化 | buzz_engagement | 暦を積む型（時刻→まさか→日付と暦→短い時間帯→合図→実績数か即時性）／「◯◯なので言いますが」型／時刻宣言型（霊視を終えました）／受付告知 |
| バズ特化 | reading_open | 霊視開始型（18:00になりました。霊視に入ります…） |
| 属人 | attract_intro | 名乗り型（土地・名前・年数・視える期間・怖いほど当たる・合図）。由来型（うたの貴船）もここ |
| 属人 | personal_note | 素の型（代弁、打ち明け、覚悟を問う、我慢の限界、今日の感覚、本音、物語） |
| 属人 | shrine_visit | 寺社訪問型（名義の県の中の寺社・麓。骨格は土地移動型と同じ、合図は県名か市名） |
| 属人 | travel_note | 土地移動型（名義の県の外。今、◯◯県にいます…『県名』とだけ残して） |
| 属人 | exclusion_hook | 不安煽り型（読み手の不安を先に言葉にして、受け止める。弾くのが目的ではない。本人 9/12「除外というより不安煽り」） |

構成の欄の語: 超バズ／バズ／霊視開始／名乗り（＝属人）／素の属人／寺社／土地移動／不安煽り。
